[module/fs-root]
type = internal/fs
mount-0 = /
format = <label>
format-mounted-prefix = FS
format-mounted-prefix-foreground = ${colors.foreground-alt}
label-mounted = / %{F#e60053}%percentage_used%%%{F-}
label-mounted-padding = 1
